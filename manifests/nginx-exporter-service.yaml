apiVersion: v1
kind: Service
metadata:
  name: nginx-exporter
  namespace: demo
  labels:
    app: nginx-exporter
spec:
  selector:
    app: nginx-exporter
  ports:
    - name: http
      port: 9113
      protocol: TCP
      targetPort: 9113
  type: ClusterIP